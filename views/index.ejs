<!DOCTYPE html>
<html>
<head>
  <title>Weather Readings</title>
  <style>
    body { font-family: Arial, sans-serif; }
    table { border-collapse: collapse; width: 50%; margin: auto; }
    th, td { padding: 8px 12px; border: 1px solid #ccc; }
    h1 { text-align: center; }
  </style>
</head>
<body>
  <h1>Latest Readings</h1>
  <table>
    <tr><th>ID</th><th>Temperature (°C)</th><th>Humidity (%)</th><th>Timestamp</th></tr>
    <% readings.forEach(r => { %>
      <tr>
        <td><%= r.id %></td>
        <td><%= r.temperature %></td>
        <td><%= r.humidity %></td>
        <td><%= r.timestamp %></td>
      </tr>
    <% }) %>
  </table>
</body>
</html>
